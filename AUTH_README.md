# Authentication System Documentation

## Обзор

Реализована полная система авторизации с использованием JWT токенов и безопасным хранением паролей.

## Функции

### Регистрация пользователей
- Минимум 3 символа для имени пользователя
- Минимум 6 символов для пароля
- Подтверждение пароля
- Автоматическое начисление стартового баланса (100💎)
- Хеширование паролей с помощью bcrypt

### Вход в систему
- Проверка учетных данных
- Выдача JWT токена (действителен 7 дней)
- Автоматическое сохранение токена в localStorage
- Автоматический вход при перезагрузке страницы

### Безопасность
- Пароли хешируются с использованием bcrypt (10 раундов соли)
- JWT токены для аутентификации без сохранения сессий
- Все WebSocket сообщения требуют авторизации
- Защищенные API endpoints
- База данных пользователей не включается в git

## API Endpoints

### POST /api/auth/register
Регистрация нового пользователя

**Request:**
```json
{
  "username": "user123",
  "password": "mypassword"
}
```

**Response:**
```json
{
  "success": true,
  "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "user": {
    "username": "user123",
    "balance": 100,
    "createdAt": "2025-11-17T...",
    "lastLogin": null
  }
}
```

### POST /api/auth/login
Вход в систему

**Request:**
```json
{
  "username": "user123",
  "password": "mypassword"
}
```

**Response:**
```json
{
  "success": true,
  "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "user": {
    "username": "user123",
    "balance": 95,
    "createdAt": "2025-11-17T...",
    "lastLogin": "2025-11-17T..."
  }
}
```

### GET /api/user/profile
Получить профиль пользователя (требует авторизации)

**Headers:**
```
Authorization: Bearer <token>
```

**Response:**
```json
{
  "username": "user123",
  "balance": 95,
  "createdAt": "2025-11-17T...",
  "lastLogin": "2025-11-17T..."
}
```

### GET /api/user/balance
Получить баланс (требует авторизации)

**Headers:**
```
Authorization: Bearer <token>
```

**Response:**
```json
{
  "balance": 95
}
```

### POST /api/user/balance/deduct
Списать средства (требует авторизации)

**Headers:**
```
Authorization: Bearer <token>
```

**Request:**
```json
{
  "amount": 5,
  "reason": "Purchase feature"
}
```

**Response:**
```json
{
  "success": true,
  "balance": 90
}
```

## WebSocket Messages

### Client → Server

#### authenticate
Аутентификация клиента при подключении

```json
{
  "type": "authenticate",
  "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
}
```

#### deduct-balance
Запрос на списание средств

```json
{
  "type": "deduct-balance",
  "amount": 10,
  "reason": "Encryption"
}
```

#### get-balance
Запрос текущего баланса

```json
{
  "type": "get-balance"
}
```

### Server → Client

#### authenticated
Успешная аутентификация

```json
{
  "type": "authenticated",
  "username": "user123",
  "balance": 95
}
```

#### auth-error
Ошибка аутентификации

```json
{
  "type": "auth-error",
  "error": "Invalid token"
}
```

#### balance-updated
Обновление баланса

```json
{
  "type": "balance-updated",
  "balance": 90,
  "success": true
}
```

## Структура базы данных

База данных хранится в файле `users.json` (исключен из git):

```json
[
  {
    "username": "user123",
    "passwordHash": "$2b$10$...",
    "balance": 95,
    "createdAt": "2025-11-17T10:00:00.000Z",
    "lastLogin": "2025-11-17T12:00:00.000Z"
  }
]
```

## Конфигурация

### Переменные окружения

Создайте файл `.env` со следующими переменными:

```env
JWT_SECRET=your-very-secret-key-change-this-in-production
PORT=3000
```

**Важно:** В продакшене обязательно измените `JWT_SECRET` на случайную строку!

## Интеграция с системой валюты

- Баланс пользователя хранится на сервере и синхронизируется с клиентом
- При регистрации начисляется начальный баланс из `currency-config.json`
- Все операции со списанием средств проходят через сервер
- Клиент использует оптимистичное обновление UI

## Использование

### Для пользователей

1. **Регистрация:**
   - Откройте приложение
   - Нажмите вкладку "Регистрация"
   - Введите имя пользователя (мин. 3 символа)
   - Введите пароль (мин. 6 символов)
   - Подтвердите пароль
   - Нажмите "Зарегистрироваться"

2. **Вход:**
   - Откройте приложение
   - Введите имя пользователя и пароль
   - Нажмите "Войти"

3. **Выход:**
   - На главном экране нажмите кнопку "Выйти"

### Для разработчиков

```javascript
// Проверка авторизации
if (this.authenticated) {
  // Пользователь авторизован
  console.log('User:', this.username);
  console.log('Balance:', this.userBalance);
}

// Списание баланса
this.deductBalance(10, 'encryption');

// Синхронизация баланса
this.syncBalance();

// Выход
this.handleLogout();
```

## Безопасность

1. **Пароли:**
   - Никогда не хранятся в открытом виде
   - Хешируются с помощью bcrypt
   - 10 раундов соли для защиты от rainbow tables

2. **Токены:**
   - JWT токены с истечением через 7 дней
   - Хранятся в localStorage (автоматическое удаление при выходе)
   - Проверяются на каждое WebSocket сообщение

3. **База данных:**
   - Исключена из системы контроля версий
   - Содержит только необходимую информацию
   - Регулярное резервное копирование рекомендуется

## Возможные улучшения

- [ ] Добавить восстановление пароля по email
- [ ] Двухфакторная аутентификация (2FA)
- [ ] История транзакций
- [ ] Ограничение количества попыток входа
- [ ] Сессии пользователей и их управление
- [ ] OAuth интеграция (Google, Telegram)
- [ ] Роли и права доступа
- [ ] Миграция на PostgreSQL/MongoDB для продакшена
