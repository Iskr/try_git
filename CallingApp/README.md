# 📱 CallingApp - iOS Application

Нативное iOS приложение для видеозвонков с поддержкой WebRTC, E2E шифрования и групповых звонков.

## 🌟 Возможности

### 🎥 Видеозвонки
- ✅ **Групповые звонки** - до 5 участников одновременно
- ✅ **WebRTC (P2P)** - прямое соединение для минимальной задержки
- ✅ **Адаптивные раскладки** - Grid, Spotlight, Sidebar, Auto режимы
- ✅ **Быстрое создание** - звонки по 6-значному коду

### 🔒 Безопасность
- ✅ **End-to-End шифрование** - AES-GCM 128-bit через CryptoKit
- ✅ **Защита данных** - шифрование аудио и видео потоков
- ✅ **Автоматическая синхронизация ключей** - передача ключей новым участникам

### 🎭 Функции
- ✅ **Система реакций** - 10 эмодзи с анимацией
- ✅ **Индивидуальная громкость** - настройка для каждого участника
- ✅ **Переключение камеры** - фронтальная/основная
- ✅ **Управление медиа** - отключение микрофона/камеры

### 🎨 Интерфейс
- ✅ **SwiftUI** - современный декларативный UI
- ✅ **Адаптивный дизайн** - оптимизация для iPhone и iPad
- ✅ **Темная тема** - поддержка Dark Mode
- ✅ **Плавные анимации** - нативные iOS переходы

## 📋 Требования

- **Xcode**: 15.0 или новее
- **iOS**: 15.0 или новее
- **Swift**: 5.9 или новее
- **CocoaPods**: для управления зависимостями

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
# Установите CocoaPods (если еще не установлен)
sudo gem install cocoapods

# Перейдите в папку проекта
cd try_git

# Установите зависимости
pod install
```

### 2. Открытие проекта

```bash
# Откройте workspace (НЕ xcodeproj!)
open CallingApp.xcworkspace
```

### 3. Настройка сервера

Откройте `CallingApp/Services/SignalingManager.swift` и замените URL сервера:

```swift
private let serverURL = "wss://your-server.com" // Замените на ваш сервер
```

### 4. Запуск приложения

1. Выберите симулятор или устройство в Xcode
2. Нажмите ⌘R или кнопку Run
3. Разрешите доступ к камере и микрофону при запросе

## 📦 Структура проекта

```
CallingApp/
├── CallingAppApp.swift          # Точка входа приложения
├── Info.plist                   # Конфигурация и разрешения
│
├── Views/                       # SwiftUI представления
│   ├── ContentView.swift        # Корневое представление
│   ├── HomeView.swift           # Главный экран
│   ├── JoinCallView.swift       # Экран присоединения
│   ├── CallView.swift           # Экран активного звонка
│   └── VideoView.swift          # Компоненты видео
│
├── ViewModels/                  # MVVM ViewModels
│   └── CallViewModel.swift      # Логика управления звонком
│
├── Services/                    # Бизнес-логика
│   ├── WebRTCManager.swift      # WebRTC соединения
│   ├── SignalingManager.swift   # WebSocket сигнализация
│   └── EncryptionManager.swift  # E2E шифрование
│
├── Models/                      # Модели данных
│   └── Participant.swift        # Участник звонка
│
└── Assets.xcassets/            # Ресурсы приложения
    └── AppIcon.appiconset/     # Иконка приложения
```

## 🔧 Зависимости

### WebRTC
```ruby
pod 'GoogleWebRTC', '~> 1.1.31999'
```
Официальный WebRTC framework от Google для iOS.

### Starscream
```ruby
pod 'Starscream', '~> 4.0.6'
```
WebSocket клиент для signaling сервера.

### RxSwift (опционально)
```ruby
pod 'RxSwift', '~> 6.6.0'
pod 'RxCocoa', '~> 6.6.0'
```
Реактивное программирование для управления состоянием.

## 🎯 Использование

### Создание звонка

1. Запустите приложение
2. Нажмите "Создать звонок"
3. Разрешите доступ к камере/микрофону
4. Скопируйте код комнаты (кнопка копирования)
5. Поделитесь кодом с участниками

### Присоединение к звонку

1. Нажмите "Присоединиться"
2. Введите 6-значный код комнаты
3. Нажмите "Присоединиться"

### Управление во время звонка

#### Основные кнопки
- **🎤 Микрофон** - включение/выключение
- **📹 Видео** - включение/выключение камеры
- **🔒 Шифрование** - активация E2E шифрования
- **😊 Реакции** - отправка эмодзи
- **📐 Раскладка** - выбор режима отображения
- **📞 Завершить** - выход из звонка

#### Дополнительные функции
- **Копировать код** - нажмите на иконку документа в заголовке
- **Громкость** - нажмите на иконку динамика у участника
- **Переключить камеру** - свайп по вашему видео (TODO)

## 🏗️ Архитектура

### MVVM Pattern

Приложение использует паттерн MVVM (Model-View-ViewModel):

```
┌─────────────┐      ┌──────────────┐      ┌────────────┐
│    View     │ ───> │  ViewModel   │ ───> │   Model    │
│  (SwiftUI)  │ <─── │  (ObservableObject)│ │  (Struct)  │
└─────────────┘      └──────────────┘      └────────────┘
                            │
                            v
                     ┌──────────────┐
                     │   Services   │
                     │ (WebRTC, etc)│
                     └──────────────┘
```

### Основные компоненты

#### WebRTCManager
Управляет WebRTC соединениями:
- Создание peer connections
- Обработка медиа-треков
- Управление ICE кандидатами
- Offer/Answer обмен

#### SignalingManager
WebSocket коммуникация:
- Подключение к signaling серверу
- Обмен SDP и ICE кандидатами
- Синхронизация участников
- Передача реакций

#### EncryptionManager
E2E шифрование:
- Генерация AES-GCM ключей
- Шифрование/дешифрование данных
- Управление ключами

#### CallViewModel
Связывает все компоненты:
- Управляет состоянием звонка
- Координирует WebRTC и Signaling
- Обновляет UI через @Published свойства

## 🔐 Безопасность

### Разрешения

Приложение запрашивает следующие разрешения (Info.plist):

```xml
<key>NSCameraUsageDescription</key>
<string>Требуется для видеозвонков</string>

<key>NSMicrophoneUsageDescription</key>
<string>Требуется для аудиозвонков</string>
```

### E2E Шифрование

1. **Генерация ключа**: AES-128 через CryptoKit
2. **Шифрование**: AES-GCM для каждого кадра
3. **Обмен ключами**: через защищенный WebSocket канал
4. **Синхронизация**: автоматическая передача новым участникам

```swift
// Пример использования
encryptionManager.generateKey()
encryptionManager.enableEncryption()

let encrypted = encryptionManager.encrypt(data: frameData)
let decrypted = encryptionManager.decrypt(data: encrypted)
```

## 🎨 Кастомизация

### Изменение цветов

В `HomeView.swift` и других вьюхах:

```swift
LinearGradient(
    gradient: Gradient(colors: [
        Color(red: 0.4, green: 0.49, blue: 0.92),  // Измените здесь
        Color(red: 0.46, green: 0.29, blue: 0.64)   // И здесь
    ]),
    startPoint: .topLeading,
    endPoint: .bottomTrailing
)
```

### Настройка WebRTC серверов

В `WebRTCManager.swift`:

```swift
config.iceServers = [
    RTCIceServer(urlStrings: ["stun:stun.l.google.com:19302"]),
    RTCIceServer(
        urlStrings: ["turn:your-turn-server.com:3478"],
        username: "username",
        credential: "password"
    )
]
```

### Добавление новых реакций

В `CallView.swift`:

```swift
let reactions = ["❤️", "👍", "😂", "😮", "😢", "🔥", "🎉", "👏", "💯", "🚀", "✨", "💪"]
```

## 🐛 Отладка

### Проверка разрешений

```swift
AVCaptureDevice.authorizationStatus(for: .video)
AVCaptureDevice.authorizationStatus(for: .audio)
```

### WebRTC логи

Включите подробное логирование в `WebRTCManager.swift`:

```swift
RTCSetMinDebugLogLevel(.verbose)
```

### Симулятор vs Реальное устройство

⚠️ **Важно**: Камера не работает на симуляторе! Для тестирования видео используйте реальное устройство.

Для симулятора можно использовать тестовые видео:

```swift
#if targetEnvironment(simulator)
// Используйте файловый источник или тестовый pattern
#endif
```

## 📱 Сборка для App Store

### 1. Настройка Bundle ID

В Xcode → Project Settings → General:
- Bundle Identifier: `com.yourcompany.callingapp`
- Version: `1.0.0`
- Build: `1`

### 2. Signing & Capabilities

- Выберите вашу команду разработчика
- Добавьте capabilities:
  - Background Modes (Audio, VoIP)
  - Push Notifications (для будущих уведомлений)

### 3. Archive & Submit

```bash
# Создайте архив
Product → Archive

# Загрузите в App Store Connect
Organizer → Distribute App
```

## 🔄 Обновление зависимостей

```bash
# Обновить все pods
pod update

# Обновить конкретный pod
pod update GoogleWebRTC
```

## 🚧 Roadmap

### В разработке
- [ ] Запись звонков
- [ ] Виртуальные фоны (CoreML)
- [ ] Демонстрация экрана
- [ ] Текстовый чат
- [ ] Push уведомления
- [ ] CallKit интеграция
- [ ] Apple Watch поддержка

### Завершено
- [x] WebRTC видеозвонки
- [x] E2E шифрование
- [x] Система реакций
- [x] Множественные раскладки
- [x] Индивидуальная громкость

## 📝 Заметки разработчика

### Работа с WebRTC на iOS

1. **Permissions**: Всегда запрашивайте разрешения до инициализации WebRTC
2. **Background**: Для работы в фоне добавьте Background Modes
3. **Memory**: WebRTC использует много памяти - следите за утечками
4. **Threading**: WebRTC callbacks могут приходить в фоновых потоках

### Оптимизация производительности

```swift
// Уменьшите разрешение для слабых устройств
let targetWidth = UIDevice.current.userInterfaceIdiom == .pad ? 1280 : 640
let targetHeight = UIDevice.current.userInterfaceIdiom == .pad ? 720 : 480
```

### Тестирование

```bash
# Запуск unit тестов
⌘U в Xcode

# UI тесты
Product → Test
```

## 🤝 Вклад

Этот проект является частью полного решения для видеозвонков. Основной репозиторий содержит также:
- Node.js signaling server
- Web приложение (HTML/JS)
- Документацию по API

## 📧 Поддержка

Если возникли вопросы:
- Создайте Issue в GitHub
- Проверьте логи в Xcode Console
- Убедитесь, что используете актуальные версии зависимостей

## 📄 Лицензия

MIT License - свободное использование в коммерческих и некоммерческих проектах.

## 🙏 Благодарности

- **Google WebRTC** - за мощный WebRTC framework
- **Starscream** - за отличный WebSocket клиент
- **Apple CryptoKit** - за нативное шифрование

---

**Сделано с ❤️ для iOS платформы**

**Минимальные требования**: iOS 15.0+ | Swift 5.9+ | Xcode 15.0+
