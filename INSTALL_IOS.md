# 📱 Инструкция по установке iOS приложения CallingApp

## Предварительные требования

### Системные требования
- **macOS**: 13.0 (Ventura) или новее
- **Xcode**: 15.0 или новее
- **iOS устройство**: iOS 15.0 или новее (для тестирования на реальном устройстве)

### Установка инструментов разработки

#### 1. Установка Xcode

```bash
# Через Mac App Store
# Откройте App Store и найдите "Xcode"
# Или используйте прямую ссылку
open "macappstore://apps.apple.com/app/xcode/id497799835"

# Установите Command Line Tools
xcode-select --install
```

#### 2. Установка CocoaPods

```bash
# Через RubyGems (рекомендуется)
sudo gem install cocoapods

# Проверка установки
pod --version
# Должно вывести: 1.x.x
```

#### Альтернатива: Homebrew

```bash
# Установите Homebrew (если еще не установлен)
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

# Установите CocoaPods
brew install cocoapods
```

## Пошаговая установка

### Шаг 1: Клонирование репозитория

```bash
# Клонируйте репозиторий
git clone https://github.com/your-username/try_git.git

# Перейдите в папку проекта
cd try_git
```

### Шаг 2: Установка зависимостей

```bash
# Установите CocoaPods зависимости
pod install

# Если возникают ошибки, попробуйте обновить репозиторий CocoaPods
pod repo update
pod install
```

**Вывод должен быть примерно таким:**
```
Analyzing dependencies
Downloading dependencies
Installing GoogleWebRTC (1.1.31999)
Installing Starscream (4.0.6)
Installing RxSwift (6.6.0)
Installing RxCocoa (6.6.0)
Generating Pods project
Integrating client project

[!] Please close any current Xcode sessions and use `CallingApp.xcworkspace` for this project from now on.
```

### Шаг 3: Открытие проекта

```bash
# ВАЖНО: Открывайте .xcworkspace, а НЕ .xcodeproj!
open CallingApp.xcworkspace
```

⚠️ **Важно**: После установки pods всегда используйте `.xcworkspace` файл!

### Шаг 4: Настройка проекта в Xcode

#### 4.1. Выбор команды разработчика

1. Откройте Xcode
2. Выберите проект `CallingApp` в навигаторе
3. Выберите target `CallingApp`
4. Перейдите на вкладку **Signing & Capabilities**
5. В разделе **Team** выберите вашу команду разработчика

> Если у вас нет Apple Developer аккаунта, можете использовать Personal Team (бесплатно)

#### 4.2. Изменение Bundle Identifier (опционально)

Если возникнут конфликты с Bundle ID:

1. На той же вкладке **Signing & Capabilities**
2. Измените **Bundle Identifier**: `com.yourname.callingapp`

### Шаг 5: Настройка сервера

Откройте файл `CallingApp/Services/SignalingManager.swift` и укажите URL вашего signaling сервера:

```swift
// Найдите строку:
private let serverURL = "wss://your-server.com"

// Замените на:
private let serverURL = "wss://actual-server-url.com"
```

#### Запуск локального сервера (для разработки)

```bash
# В отдельном терминале, в корне проекта
npm install
npm start

# Сервер запустится на http://localhost:3000
# WebSocket будет доступен на ws://localhost:3000
```

Затем в SignalingManager используйте:
```swift
private let serverURL = "ws://localhost:3000"  // Для симулятора
// или
private let serverURL = "ws://YOUR_IP:3000"    // Для реального устройства
```

### Шаг 6: Сборка и запуск

#### На симуляторе

1. В Xcode выберите симулятор: **iPhone 15 Pro** (или любой другой)
2. Нажмите **⌘R** или кнопку **Run** (▶️)

⚠️ **Ограничение**: Камера не работает на симуляторе, но вы можете тестировать UI и логику

#### На реальном устройстве

1. Подключите iPhone/iPad через USB
2. Разблокируйте устройство
3. В Xcode выберите ваше устройство
4. Нажмите **⌘R** или кнопку **Run**

**При первом запуске:**
1. На устройстве: Settings → General → VPN & Device Management
2. Найдите ваш профиль разработчика
3. Нажмите **Trust**

### Шаг 7: Предоставление разрешений

При первом запуске приложение запросит разрешения:

1. **Камера** - нажмите "Allow" (Разрешить)
2. **Микрофон** - нажмите "Allow" (Разрешить)

Если случайно отказали:
- Settings → CallingApp → включите Camera и Microphone

## 🔧 Решение проблем

### Проблема: "No such module 'WebRTC'"

```bash
# Решение 1: Очистите и переустановите pods
pod deintegrate
pod install

# Решение 2: Очистите Derived Data
rm -rf ~/Library/Developer/Xcode/DerivedData

# Затем в Xcode:
Product → Clean Build Folder (⇧⌘K)
```

### Проблема: "Command PhaseScriptExecution failed"

```bash
# Проверьте, установлен ли CocoaPods
which pod

# Переустановите CocoaPods
sudo gem uninstall cocoapods
sudo gem install cocoapods

# Переустановите зависимости
pod install
```

### Проблема: Ошибки подписи кода

1. В Xcode: Signing & Capabilities
2. Снимите галочку "Automatically manage signing"
3. Затем снова поставьте галочку
4. Выберите вашу команду

### Проблема: "Pods project not found"

```bash
# Убедитесь, что вы в правильной директории
ls -la
# Должен быть файл Podfile

# Запустите установку
pod install
```

### Проблема: WebSocket не подключается

#### На симуляторе:
```swift
// Используйте localhost
private let serverURL = "ws://localhost:3000"
```

#### На реальном устройстве:
```swift
// Используйте IP адрес вашего компьютера
private let serverURL = "ws://192.168.1.100:3000"  // Замените на ваш IP

// Найти IP: System Preferences → Network → выберите активное соединение
```

**Важно**: Устройство и компьютер должны быть в одной сети!

### Проблема: Низкая производительность

```swift
// В WebRTCManager.swift измените разрешение
let targetWidth = 640   // было 1280
let targetHeight = 480  // было 720
```

## 🚀 Дополнительная настройка

### Включение Background Modes

Для работы звонков в фоновом режиме:

1. Xcode → Target → Signing & Capabilities
2. Нажмите **+ Capability**
3. Выберите **Background Modes**
4. Включите:
   - ✅ Audio, AirPlay, and Picture in Picture
   - ✅ Voice over IP

### Добавление Push Notifications

1. Добавьте capability: **Push Notifications**
2. В AppDelegate добавьте регистрацию для notifications

### Настройка App Icon

1. Подготовьте иконки (1024x1024 и другие размеры)
2. Откройте `Assets.xcassets/AppIcon.appiconset`
3. Перетащите иконки в соответствующие слоты

## 📱 Тестирование

### Unit тесты

```bash
# В Xcode нажмите
⌘U

# Или через терминал
xcodebuild test -workspace CallingApp.xcworkspace -scheme CallingApp -destination 'platform=iOS Simulator,name=iPhone 15 Pro'
```

### UI тесты

```bash
# В Xcode:
Product → Test

# Выберите конкретный тест
Ctrl + Option + ⌘ + U
```

### Тестирование с несколькими устройствами

1. Запустите приложение на устройстве 1
2. Создайте звонок, скопируйте код
3. Запустите приложение на устройстве 2 (или в браузере web версии)
4. Присоединитесь по коду

## 📊 Мониторинг

### Просмотр логов

В Xcode:
- **⌘⇧Y** - открыть консоль
- Фильтр: поиск по "WebRTC", "Signaling", "Encryption"

### Инструменты профилирования

```bash
# В Xcode:
Product → Profile (⌘I)

# Выберите инструмент:
- Time Profiler (производительность)
- Allocations (память)
- Network (сеть)
```

## 🎓 Дополнительные ресурсы

### Документация
- [WebRTC iOS Documentation](https://webrtc.googlesource.com/src/+/refs/heads/main/sdk/objc/README.md)
- [Apple Developer Documentation](https://developer.apple.com/documentation/)
- [CocoaPods Guides](https://guides.cocoapods.org/)

### Полезные команды

```bash
# Очистка
pod cache clean --all
rm -rf ~/Library/Caches/CocoaPods

# Обновление pods
pod update
pod outdated

# Информация о pod
pod spec cat GoogleWebRTC

# Проверка Podfile
pod lib lint
```

## ✅ Чек-лист готовности

Перед запуском убедитесь:

- [ ] Установлен Xcode 15.0+
- [ ] Установлен CocoaPods
- [ ] Выполнено `pod install`
- [ ] Открыт `.xcworkspace` файл
- [ ] Выбрана команда разработчика
- [ ] Настроен URL сервера
- [ ] Запущен signaling сервер (если локальный)
- [ ] Разрешения Camera/Microphone предоставлены

## 🎉 Готово!

Теперь вы можете:
1. Создавать звонки
2. Присоединяться к звонкам
3. Использовать E2E шифрование
4. Отправлять реакции
5. Настраивать громкость участников

Приятного использования! 🚀
